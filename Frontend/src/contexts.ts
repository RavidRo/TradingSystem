import React from 'react';

export const CookieContext = React.createContext('');
export const AdminsContext = React.createContext<string[]>([]);
export const UsernameContext = React.createContext('');
